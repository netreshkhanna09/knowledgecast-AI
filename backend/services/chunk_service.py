# Text chunking
# This will use LangChain RecursiveCharacterTextSplitter

def chunk_text(text: str, chunk_size: int = 700, overlap: int = 100) -> list:
    """Split text into overlapping chunks and return list of chunks."""
    pass  # implementation comes on day 8